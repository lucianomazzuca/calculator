Create function add, substract, multiply and divide

function operate(num1, num1, operator)

Get input:
addEventListener button (get innerHTML value)
Save in variable
Display variable



Get operan1:
    addEventListener button (get innerHTML value)
    Save in variable
    Display variable

Once operator != ''
    get operan2:
        addEventListener button (get innerHTML value)
        Save in variable
        Display variable
    