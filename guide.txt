Create function add, substract, multiply and divide

function operate(num1, num1, operator)

Get input:
addEventListener button (get innerHTML value)
Save in variable
Display variable


Get operan1:
    addEventListener button (get innerHTML value)
    Save in variable
    Display variable

Once operator != ''
    get operan2:
        addEventListener button (get innerHTML value)
        Save in variable
        Display variable
    

delete:
    if operator == ''
        deleteLastValue()
        updateDisplay
        firstOperand = display.innerText 
    else if operator != ''
        deleteLastValue()
        updateDisplay
        secondOperand = display.innerText 

result:
    if(secondOperand != '') then
        result = operate(firstOperand, secondOperand)
        display(result)
        clean firstOperand, secondOperand, operator.
    
delete operator:
